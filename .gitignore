# OS / editor
.DS_Store
Thumbs.db
*.log

# Dependencies (none required — this is a zero-build static site)
node_modules/

# Local server cache
.cache/

# Local image build scripts (Pillow / Windows) — generated assets are committed
_*.py

# Python bytecode cache (from running/compiling the local build scripts)
__pycache__/

# 로컬 전용 핸드오프 노트(공개 repo 제외)
CLAUDE.md
